console.log(nameOfdog); // se llama una variable que no se está declarando aún, si se ejecuta este código arrojaría error
var nameOfdog = "Elmo"; // aquí se hace hoisting de una variable que aunque no se ha asignado valor ya está disponible con el valor de undefined
