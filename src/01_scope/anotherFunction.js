const anotherFunction = () => {
  var x = 1; // se crea una variable y se inicializa en 1
  var x = 2; // se reasigna el valor de la variable
  let y = 1; // se crea una variable y y se asigna el valor  de 1
  //let y = 2; Esta línea no funciona porque no puede volver a declcararse la misma variable

  console.log(x);
  console.log(y);
};

anotherFunction();
